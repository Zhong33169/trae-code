# 换表申请管理系统

换表申请全流程管理系统，覆盖申请登记、过程核验、复核归档等环节，支持角色权限、异常检测、审计日志等功能。

## 技术栈

- **前端**: TanStack Start (React)，端口 3004
- **后端**: Node.js + Hono，端口 8004
- **数据库**: SQLite (better-sqlite3)

## 目录结构

```
├── backend/              # 后端项目
│   ├── src/
│   │   ├── db/           # 数据库相关
│   │   ├── middleware/   # 中间件
│   │   ├── routes/       # API 路由
│   │   ├── utils/        # 工具函数
│   │   └── index.js      # 后端入口
│   └── data/             # SQLite 数据文件目录
├── frontend/             # 前端项目
│   └── app/
│       ├── routes/       # 页面路由
│       ├── components/   # 组件
│       ├── lib/          # 工具库
│       └── styles/       # 样式
└── README.md
```

## 快速开始

### 1. 启动后端

```bash
cd backend
npm install
npm run init-db   # 初始化数据库和演示数据
npm run dev       # 启动开发服务器 (端口 8004)
```

后端 API 地址: `http://localhost:8004/api`

### 2. 启动前端

```bash
cd frontend
npm install
npm run dev       # 启动开发服务器 (端口 3004)
```

前端访问地址: `http://localhost:3004`

## 系统角色

| 角色 | 用户名 | 权限 |
|------|--------|------|
| 换表登记员 | meter_operator (张三) | 发起/补正申请、上传附件 |
| 换表审核主管 | meter_supervisor (李四) | 审核通过/退回、上传附件 |
| 燃气服务公司复核负责人 | gas_archivist (王五) | 复核归档、批量归档、上传附件 |

在页面右上角可切换当前登录角色。

## 业务流程

```
换表登记员发起申请 → 待审核主管办理
    ↓
换表审核主管审核
    ├─ 通过 → 待复核负责人归档
    └─ 退回 → 待登记员补正（可重新提交）
    ↓
燃气服务公司复核负责人复核归档 → 已归档（完成）
```

## 演示数据说明

系统预置 10 条样例申请，覆盖 7 种场景类型，每条申请均有明确的「样例类型」标签和审计备注。

### 🟢 正常样例（sample_case: normal）

| 申请编号 | 状态 | 批次号 | 说明 | 操作入口 |
|----------|------|--------|------|----------|
| **APP001** | 待审核主管办理 | BATCH202501001 | 正常流转中，待主管审核 | 切换到「换表审核主管」→ 审核通过 |
| **APP002** | 待复核负责人归档 | BATCH202501001 | 正常流转中，待复核归档 | 切换到「燃气服务公司复核负责人」→ 复核归档 |
| **APP005** | 已归档 | BATCH202501003 | ✅ 完整流程样例，已完成归档 | 点击查看详情，审计日志可追溯全流程 |
| **APP009** | 已归档 | BATCH202501009 | ✅ 正常归档，用于制造 APP010 批次冲突 | 点击查看详情 |

### 🟠 异常样例

#### 1. 缺材料（sample_case: missing_materials）
| 申请编号 | 状态 | 批次号 | 说明 | 操作入口 |
|----------|------|--------|------|----------|
| **APP004** | 待审核主管办理 | BATCH202501004 | 缺材料：未上传新表安装记录和现场照片 | 切换到「换表审核主管」→ 退回 → 切换到「换表登记员」→ 补正提交 |

#### 2. 超时（sample_case: timeout）
| 申请编号 | 状态 | 批次号 | 说明 | 操作入口 |
|----------|------|--------|------|----------|
| **APP006** | 待复核负责人归档 | BATCH202501006 | 超时：SLA 截止 2025-01-14，已超时 | 切换到「燃气服务公司复核负责人」→ 复核归档 |

#### 3. 退回（sample_case: rejected）
| 申请编号 | 状态 | 批次号 | 说明 | 操作入口 |
|----------|------|--------|------|----------|
| **APP003** | 待登记员补正 | BATCH202501002 | 已退回：缺少旧表照片和换表现场记录 | 切换到「换表登记员」→ 补正提交 → 主管审核 |

#### 4. 离线台账缺失（sample_case: offline_missing）
| 申请编号 | 状态 | 批次号 | 线上线下差异 | 操作入口 |
|----------|------|--------|-------------|----------|
| **APP007** | 待复核负责人归档 | BATCH202501007 | 线上有申请，但**离线台账无此记录**（旧表号 OLD-007） | 切换到「燃气服务公司复核负责人」→ 复核归档 → 系统弹窗提示台账缺失，申请保持待处理 |

#### 5. 状态不一致（sample_case: status_inconsistency）
| 申请编号 | 状态 | 批次号 | 线上线下差异 | 操作入口 |
|----------|------|--------|-------------|----------|
| **APP008** | 待复核负责人归档 | BATCH202501008 | 线上：待归档；**线下台账已作废**（旧表号 OLD-008） | 切换到「燃气服务公司复核负责人」→ 复核归档 → 系统弹窗提示状态不一致 |

#### 6. 重复批次（sample_case: batch_conflict）
| 申请编号 | 状态 | 批次号 | 线上线下差异 | 操作入口 |
|----------|------|--------|-------------|----------|
| **APP010** | 待复核负责人归档 | BATCH202501009 | 同批次号已被 APP009（旧表号 OLD-009）占用，当前表号 OLD-010 | 切换到「燃气服务公司复核负责人」→ 复核归档 → 系统弹窗提示批次冲突 |

### 样例导览提示
- 列表页「样例类型」列显示每条申请的场景标签（彩色标签）
- 列表页「下一步办理人」列显示当前待办角色和操作
- 详情页顶部「流程导览」区域展示：
  - ⚠️ **预期异常**：红色卡片，说明线上线下差异点
  - 🔵 **下一步操作**：蓝色卡片，说明办理人和操作指引
- 所有样例的审计记录均包含明确的处理人、角色、动作和备注

## 核心功能

### 1. 申请列表
- 按状态筛选（待登记员补正/待审核主管办理/待复核负责人归档/已退回/已归档）
- 按异常筛选（仅显示有问题的申请）
- 按批次号、关键词搜索
- 异常申请行高亮显示（红色背景）
- 批量归档（仅复核负责人角色可见）

### 2. 申请详情
- 基本信息、表计信息展示
- **经办人员展示**: 清晰显示换表登记员（发起）、换表审核主管（办理）、燃气服务公司复核负责人（归档）三个角色及其处理人
- 附件管理: 上传、删除
- 流程记录: 时间线展示每个节点的操作人、角色、意见、退回原因、处理结果
- **离线台账回填失败原因**: 若归档时台账校验失败，在详情页顶部红色提示框显示
- **审计备注**: 可编辑，在详情页显示
- **审计日志**: 记录所有操作，包含失败原因

### 3. 异常校验机制
- **离线台账缺失检测**: 归档时校验批次号+旧表号是否在离线台账中存在
- **批次号重复检测**: 同一批次号被不同表号使用时报警
- 校验失败时:
  - 弹窗提示具体错误信息（说明线上/线下哪里对不上）
  - 可选择「强制归档」继续流程
  - 失败原因记录到申请详情和审计日志

### 4. 批量归档
- 复核负责人可在列表页勾选多条申请批量归档
- 结果弹窗逐条说明：成功/台账匹配成功、失败/台账匹配失败原因
- 批量操作记录到审计日志

### 5. 审计日志
- 独立页面查看所有操作记录
- 支持筛选仅显示失败记录
- 每条记录包含: 操作人、角色、操作类型、关联申请、详情、失败原因
- 可追溯谁、在什么时候、为什么处理失败

## API 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/health | 健康检查 |
| GET | /api/users | 获取用户列表 |
| GET | /api/applications | 获取申请列表（支持 status/abnormal/batch_no/keyword 参数） |
| GET | /api/applications/:id | 获取申请详情 |
| POST | /api/applications | 新建申请（登记员） |
| POST | /api/applications/:id/submit | 提交审核/补正提交（登记员） |
| POST | /api/applications/:id/approve | 审核通过（主管） |
| POST | /api/applications/:id/reject | 退回申请（主管，需 reject_reason） |
| POST | /api/applications/:id/archive | 复核归档（负责人，支持 force_archive） |
| POST | /api/applications/batch-archive | 批量归档（负责人） |
| POST | /api/applications/:id/attachments | 上传附件 |
| DELETE | /api/applications/:id/attachments/:attachId | 删除附件 |
| POST | /api/applications/:id/audit-remark | 更新审计备注 |
| GET | /api/applications/validate/ledger | 校验离线台账（batch_no, old_meter_no 参数） |
| GET | /api/audit | 获取审计日志 |
| GET | /api/audit/failures | 获取失败审计日志 |

所有接口需在请求头中携带 `X-User-Id` 指定当前用户 ID。

## 验收测试清单

- [ ] 正常单完整流程: 发起 → 审核 → 归档
- [ ] 缺材料单: 退回 → 补正 → 重新提交 → 通过 → 归档
- [ ] 超时单/退回单: 查看退回原因、补正提交
- [ ] 批量归档: 逐条结果说明
- [ ] 离线台账缺失: 系统停住并说明线上线下不一致处
- [ ] 重复批次: 系统检测冲突并提示
- [ ] 审计日志: 失败原因可追溯到操作人、时间
- [ ] 角色切换: 按钮和流程根据角色正确显示/隐藏，经办人员信息清晰展示
