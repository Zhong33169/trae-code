{
  "name": "transfer-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port ${FRONTEND_PORT:-3005}",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@web/dev-server-esbuild": "^1.0.2",
    "vite": "^6.0.0",
    "typescript": "^5.7.0"
  }
}
