{
  "name": "medical-events-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "PORT=${PORT:-3004} vinxi dev --port ${PORT:-3004}",
    "build": "vinxi build",
    "start": "PORT=${PORT:-3004} vinxi start --port ${PORT:-3004}"
  },
  "dependencies": {
    "@solidjs/meta": "^0.29.4",
    "@solidjs/router": "^0.15.3",
    "@solidjs/start": "^1.1.3",
    "solid-js": "^1.9.5",
    "vinxi": "^0.5.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
